#include <stdio.h>
#include <conio.h>

int main(){
    int a;
    float b;
    printf("Enter the integer and float numbers:");
    scanf("%d%f",&a,&b);
    printf("The Entered nos are %d and %f",a,b);
    return 0;
}